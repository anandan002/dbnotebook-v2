# PowerShell Scripts

Place repository PowerShell automation scripts (`*.ps1`) in this directory.

Current script:
- `dbnotebook-service.ps1`: installs and manages DBNotebook as a Windows service.
- Uses NSSM as the Windows service wrapper backend.
